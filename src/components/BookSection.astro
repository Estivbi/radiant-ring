---
interface Book {
  id: string;
  title: string;
  cover: string;
  description: string;
  price: number;
  buyLink: string;
}

interface Props {
  books: Book[];
}

const { books } = Astro.props;
---

<section id="libros" class="p-16 md:p-20">
  <div class="container mx-auto p-4">
    <div class="text-center mb-12">
      <h2 class="text-4xl md:text-5xl mb-4 text-transparent bg-clip-text bg-gradient-to-r from-green-400 to-blue-500">
        Mis Libros
      </h2>
      <p class="text-xl text-blue-200 max-w-3xl mx-auto">
        Explora mis universos fantásticos y déjate llevar por historias llenas de aventuras, personajes inolvidables y mucho humor friki.
      </p>
    </div>
    
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {books.map((book) => (
        <div class="bg-white/10 backdrop-blur-lg rounded-lg overflow-hidden border border-white/20 transition-all duration-300 hover:shadow-lg hover:shadow-purple-500/20 hover:border-white/30 group">
          <div class="relative overflow-hidden aspect-[2/3]">
            <img 
              src={book.cover} 
              alt={`Portada del libro ${book.title}`}
              class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent flex items-end">
              <div class="p-6">
                <h3 class="text-2xl font-bold mb-2">{book.title}</h3>
                <p class="text-blue-200 mb-4 line-clamp-2">{book.description}</p>
                <div class="flex items-center justify-between">
                  <span class="text-xl font-bold text-green-400">{book.price.toFixed(2)}€</span>
                  <a href={book.buyLink} class="inline-block p-2 rounded-full font-bold bg-gradient-to-r from-green-500 to-teal-500 text-white hover:from-green-600 hover:to-teal-700">Comprar</a>
                </div>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>